<!-- 
Title: fruit-list.ejs (https://github.com/buwebdev/web-340/blob/master/week-7/fms/views/fruit-list.ejs)
Author: Richard Krasso 
Date: 2/18/2023
Description: An EJS example page that helped me structure the elements of my own pages
-->

<!-- the header partial used on all pages -->
<%- include('./partials/_header.ejs') %>

<section class="container">
  <div class="card center-div">
    <div class="card-title">Customer List</div>

    <div class="card-content">
      <% if(customers.length > 0) { %>
      <table class="table">
        <tr>
          <th>Customer ID</th>
          <th>Email</th>
          <th></th>
        </tr>
        <tbody>
          <% customers.forEach(function(customer) { %>
          <tr>
            <td><%= customer.customerId %></td>
            <td><%= customer.email %></td>
            <td>
              <a href="#">DELETE</a>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
      <% } else { %>
      <h3>
        Customers have not been added to the MongoDB database. Try adding some!
      </h3>
      <% } %>
    </div>
  </div>
</section>

<!-- the footer partial used on all pages -->
<%- include('./partials/_footer.ejs') %>
